<div class="store-index">
  <% if @stores.present? %>
    <div class="store-items">
      <% @stores.each do |store| %>
        <div class="store-item">
          <%= link_to store_path(store) do %>
            <%= store.name %><br>
            <%= store.owner_name %><br>
            <%= store.cpf %><br>
          <% end %>
        </div>
      <% end %>
    </div>
  <% else %>
    <h2>Ainda não existe transações, importe um CNAB.</h2>
  <% end %>
</div>